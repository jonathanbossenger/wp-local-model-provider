<?xml version="1.0"?>
<ruleset name="WP Ollama Model Provider">
    <description>WordPress Coding Standards for WP Ollama Model Provider</description>

    <!-- Check all PHP files in the plugin -->
    <file>.</file>

    <!-- Exclude vendor and other directories -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/tests/*</exclude-pattern>

    <!-- Use WordPress Coding Standards -->
    <rule ref="WordPress">
        <!-- Allow short array syntax -->
        <exclude name="Generic.Arrays.DisallowShortArraySyntax"/>
    </rule>

    <!-- Allow for theme-specific naming conventions -->
    <rule ref="WordPress.Files.FileName">
        <properties>
            <property name="strict_class_file_names" value="false"/>
        </properties>
    </rule>

    <!-- Enforce PSR-4 naming for classes -->
    <rule ref="WordPress.Files.FileName.InvalidClassFileName">
        <exclude-pattern>*/includes/*</exclude-pattern>
    </rule>

    <!-- Check for PHP cross-version compatibility -->
    <config name="testVersion" value="8.0-"/>

    <!-- Show progress -->
    <arg value="ps"/>

    <!-- Show colors in output -->
    <arg name="colors"/>

    <!-- Run in parallel for faster checking -->
    <arg name="parallel" value="8"/>

    <!-- Include the WordPress-Extra standard -->
    <rule ref="WordPress-Extra"/>

    <!-- Include WordPress-Docs standard -->
    <rule ref="WordPress-Docs"/>
</ruleset>
